/* 空状态 */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 16rpx;
  padding: 40rpx 0;
}
.empty-icon {
  font-size: 80rpx;
  opacity: 0.5;
}
.empty-text {
  font-size: 26rpx;
  color: var(--text-tertiary);
}
